# M15 — Advanced Core Features

> Источник: `packages/core/TODO.md`

> **Задач:** 92 | **Проверка:** Causal analysis, drift, predictions, reports в domain

> **Результат milestone:** Готовы advanced-домены: causal analysis, drift, predictive analytics и executive reports.

## v0.52.0 — Causal Analysis: Temporal Coupling & Bug Tracking

> Value objects и порты для temporal coupling, bug introduction tracking.

> **Результат версии:** Завершена версия «v0.52.0 — Causal Analysis: Temporal Coupling & Bug Tracking» в рамках M15; инкремент готовит продукт к стабильному end-to-end сценарию в своем слое.

| ID | Задача | Статус | Результат | Acceptance Criteria |
|----------|--------------------|--------|-----------|---------------------|
| CORE-283 | Реализовать temporalCoupling VO | TODO | Не начато | Реализация: `domain/value-objects/analysis/temporal-coupling.ts`. Extends ValueObject. Props: `{fileA: FilePath, fileB: FilePath, couplingStrength: number, commitCount: number, confidence: number, timeWindowDays: number}`. Валидация: strength в [0,1], commitCount >= 1, confidence в [0,1], fileA !== fileB. isStrong() (strength > 0.7). Готово, если: для CORE-283 полностью выполнены пункты блока «Реализация», поведение подтверждено unit/integration тестами (happy-path, negative, edge-case), контракты DTO/ports совместимы, регрессии в смежных use case отсутствуют; DoD: `cd packages/core && bun run lint && bun run typecheck && bun test`. |
| CORE-284 | Реализовать temporalCoupling DTO | TODO | Не начато | Реализация: `application/dto/analytics/temporal-coupling.dto.ts`. `ITemporalCoupling`: `{fileA, fileB, couplingStrength, commitCount, confidence, timeWindowDays, lastCoChanged}`. Тесты типов. Готово, если: для CORE-284 полностью выполнены пункты блока «Реализация», поведение подтверждено unit/integration тестами (happy-path, negative, edge-case), контракты DTO/ports совместимы, регрессии в смежных use case отсутствуют; DoD: `cd packages/core && bun run lint && bun run typecheck && bun test`. |
| CORE-285 | Реализовать iTemporalCouplingService порт | TODO | Не начато | Реализация: `application/ports/outbound/analysis/temporal-coupling-service.ts`. `analyzeCoupling(repoId, options?): Promise<ITemporalCoupling[]>`, `getCouplingForFile(repoId, filePath)`, `getStrongestCouplings(repoId, limit)`. ITemporalCouplingOptions: `{timeWindowDays?, minCommitCount?, minStrength?}`. Токен `TOKENS.Analysis.TemporalCouplingService`. Готово, если: для CORE-285 полностью выполнены пункты блока «Реализация», поведение подтверждено unit/integration тестами (happy-path, negative, edge-case), контракты DTO/ports совместимы, регрессии в смежных use case отсутствуют; DoD: `cd packages/core && bun run lint && bun run typecheck && bun test`. |
| CORE-286 | Реализовать bugIntroduction DTO | TODO | Не начато | Реализация: `application/dto/analytics/bug-introduction.dto.ts`. `IBugIntroduction`: `{id, commitSha, filePath, introducedAt, detectedAt, bugType: BugType, severity, fixCommitSha?, fixedAt?, timeToFix?}`. BugType: REGRESSION/NEW_BUG/REINTRODUCTION/LATENT. Тесты типов. Готово, если: для CORE-286 полностью выполнены пункты блока «Реализация», поведение подтверждено unit/integration тестами (happy-path, negative, edge-case), контракты DTO/ports совместимы, регрессии в смежных use case отсутствуют; DoD: `cd packages/core && bun run lint && bun run typecheck && bun test`. |
| CORE-287 | Реализовать iBugTracker порт | TODO | Не начато | Реализация: `application/ports/outbound/analysis/bug-tracker.ts`. `trackIntroduction, getIntroductions(options?), getIntroductionsByFile, getIntroductionsByCommit, markFixed(bugId, fixCommitSha)`. Токен `TOKENS.Analysis.BugTracker`. Barrel export. Готово, если: для CORE-287 полностью выполнены пункты блока «Реализация», поведение подтверждено unit/integration тестами (happy-path, negative, edge-case), контракты DTO/ports совместимы, регрессии в смежных use case отсутствуют; DoD: `cd packages/core && bun run lint && bun run typecheck && bun test`. |
| CORE-288 | Реализовать analyzeTemporalCouplingUseCase | TODO | Не начато | Реализация: `application/use-cases/analytics/analyze-temporal-coupling.use-case.ts`. IUseCase<{repoId, timeWindowDays?, minCommitCount?, minStrength?}, ITemporalCoupling[], DomainError>. Вызывает ITemporalCouplingService. Готово, если: для CORE-288 полностью выполнены пункты блока «Реализация», поведение подтверждено unit/integration тестами (happy-path, negative, edge-case), контракты DTO/ports совместимы, регрессии в смежных use case отсутствуют; DoD: `cd packages/core && bun run lint && bun run typecheck && bun test`. |
| CORE-289 | Реализовать trackBugIntroductionUseCase | TODO | Не начато | Реализация: `application/use-cases/analytics/track-bug-introduction.use-case.ts`. IUseCase<{repoId, commitSha, filePath, bugType, severity}, IBugIntroduction, DomainError>. Создаёт и сохраняет через IBugTracker. Готово, если: для CORE-289 полностью выполнены пункты блока «Реализация», поведение подтверждено unit/integration тестами (happy-path, negative, edge-case), контракты DTO/ports совместимы, регрессии в смежных use case отсутствуют; DoD: `cd packages/core && bun run lint && bun run typecheck && bun test`. |

---

## v0.53.0 — Causal Analysis: Root Cause Chains & Health Trends

> Root cause chain analysis, code health trends, churn-complexity correlation, regression detection.

> **Результат версии:** Завершена версия «v0.53.0 — Causal Analysis: Root Cause Chains & Health Trends» в рамках M15; инкремент готовит продукт к стабильному end-to-end сценарию в своем слое.

| ID | Задача | Статус | Результат | Acceptance Criteria |
|----------|--------------------|--------|-----------|---------------------|
| CORE-290 | Реализовать rootCauseChain DTO | TODO | Не начато | Реализация: `application/dto/analytics/root-cause-chain.dto.ts`. `IRootCauseChain`: `{chainId, issueId, ccrId?, commitSha, taskId?, filePath, links: IRootCauseLink[]}`. `IRootCauseLink`: `{type: RootCauseLinkType, sourceId, targetId, metadata?}`. RootCauseLinkType: ISSUE_TO_CCR/CCR_TO_COMMIT/COMMIT_TO_TASK/COMMIT_TO_FILE/FILE_TO_BUG. Готово, если: для CORE-290 полностью выполнены пункты блока «Реализация», поведение подтверждено unit/integration тестами (happy-path, negative, edge-case), контракты DTO/ports совместимы, регрессии в смежных use case отсутствуют; DoD: `cd packages/core && bun run lint && bun run typecheck && bun test`. |
| CORE-291 | Реализовать codeHealthTrend DTO | TODO | Не начато | Реализация: `application/dto/analytics/code-health-trend.dto.ts`. `ICodeHealthTrend`: `{filePath, dataPoints: IHealthDataPoint[], trendDirection: TrendDirection, trendSlope, overallScore}`. `IHealthDataPoint`: `{date, score, complexity, churn, issueCount, coverage?}`. TrendDirection: IMPROVING/DEGRADING/STABLE. Тесты типов. Готово, если: для CORE-291 полностью выполнены пункты блока «Реализация», поведение подтверждено unit/integration тестами (happy-path, negative, edge-case), контракты DTO/ports совместимы, регрессии в смежных use case отсутствуют; DoD: `cd packages/core && bun run lint && bun run typecheck && bun test`. |
| CORE-292 | Реализовать churnComplexityCorrelation DTO | TODO | Не начато | Реализация: `application/dto/analytics/churn-complexity-correlation.dto.ts`. `IChurnComplexityCorrelation`: `{filePath, churn, complexity, quadrant: ChurnComplexityQuadrant, riskLevel}`. Quadrant: TRIVIAL/BORING/COMPLEX_STABLE/HOTSPOT. HOTSPOT = high churn + high complexity. Тесты типов. Готово, если: для CORE-292 полностью выполнены пункты блока «Реализация», поведение подтверждено unit/integration тестами (happy-path, negative, edge-case), контракты DTO/ports совместимы, регрессии в смежных use case отсутствуют; DoD: `cd packages/core && bun run lint && bun run typecheck && bun test`. |
| CORE-293 | Реализовать regressionPattern DTO | TODO | Не начато | Реализация: `application/dto/analytics/regression-pattern.dto.ts`. `IRegressionPattern`: `{patternId, signature, occurrences: IRegressionOccurrence[], frequency, firstSeen, lastSeen, affectedFiles[]}`. `IRegressionOccurrence`: `{commitSha, date, filePath, description}`. Тесты типов. Готово, если: для CORE-293 полностью выполнены пункты блока «Реализация», поведение подтверждено unit/integration тестами (happy-path, negative, edge-case), контракты DTO/ports совместимы, регрессии в смежных use case отсутствуют; DoD: `cd packages/core && bun run lint && bun run typecheck && bun test`. |
| CORE-294 | Реализовать iCausalAnalysisService порт | TODO | Не начато | Реализация: `application/ports/outbound/analysis/causal-analysis-service.ts`. `buildRootCauseChain(repoId, issueId), calculateHealthTrend(repoId, filePath, timeRangeDays), correlateChurnComplexity(repoId, filePaths[]), detectRegressionPatterns(repoId, options?)`. Токен `TOKENS.Analysis.CausalAnalysisService`. Готово, если: для CORE-294 полностью выполнены пункты блока «Реализация», поведение подтверждено unit/integration тестами (happy-path, negative, edge-case), контракты DTO/ports совместимы, регрессии в смежных use case отсутствуют; DoD: `cd packages/core && bun run lint && bun run typecheck && bun test`. |
| CORE-295 | Реализовать buildRootCauseChainUseCase | TODO | Не начато | Реализация: IUseCase<{repoId, issueId}, IRootCauseChain, DomainError>. NotFoundError если chain не найден. Готово, если: для CORE-295 полностью выполнены пункты блока «Реализация», поведение подтверждено unit/integration тестами (happy-path, negative, edge-case), контракты DTO/ports совместимы, регрессии в смежных use case отсутствуют; DoD: `cd packages/core && bun run lint && bun run typecheck && bun test`. |
| CORE-296 | Реализовать calculateCodeHealthTrendUseCase | TODO | Не начато | Реализация: IUseCase<{repoId, filePath, timeRangeDays?}, ICodeHealthTrend, DomainError>. Default timeRangeDays = 90. Готово, если: для CORE-296 полностью выполнены пункты блока «Реализация», поведение подтверждено unit/integration тестами (happy-path, negative, edge-case), контракты DTO/ports совместимы, регрессии в смежных use case отсутствуют; DoD: `cd packages/core && bun run lint && bun run typecheck && bun test`. |
| CORE-297 | Реализовать correlateChurnComplexityUseCase | TODO | Не начато | Реализация: IUseCase<{repoId, filePaths?}, IChurnComplexityCorrelation[], DomainError>. Если filePaths не указаны — загружает все файлы из IGraphRepository. Готово, если: для CORE-297 полностью выполнены пункты блока «Реализация», поведение подтверждено unit/integration тестами (happy-path, negative, edge-case), контракты DTO/ports совместимы, регрессии в смежных use case отсутствуют; DoD: `cd packages/core && bun run lint && bun run typecheck && bun test`. |
| CORE-298 | Реализовать detectRegressionPatternUseCase | TODO | Не начато | Реализация: IUseCase<{repoId, minOccurrences?, timeWindowDays?}, IRegressionPattern[], DomainError>. Defaults: minOccurrences=2, timeWindowDays=180. Готово, если: для CORE-298 полностью выполнены пункты блока «Реализация», поведение подтверждено unit/integration тестами (happy-path, negative, edge-case), контракты DTO/ports совместимы, регрессии в смежных use case отсутствуют; DoD: `cd packages/core && bun run lint && bun run typecheck && bun test`. |

---

## v0.54.0 — Causal Overlays: DTOs for CodeCity

> Overlay DTOs для CodeCity causal визуализаций.

> **Результат версии:** Завершена версия «v0.54.0 — Causal Overlays: DTOs for CodeCity» в рамках M15; инкремент готовит продукт к стабильному end-to-end сценарию в своем слое.

| ID | Задача | Статус | Результат | Acceptance Criteria |
|----------|--------------------|--------|-----------|---------------------|
| CORE-299 | Реализовать temporalCouplingOverlay DTO | TODO | Не начато | Реализация: `application/dto/analytics/temporal-coupling-overlay.dto.ts`. `ITemporalCouplingOverlay`: `{lines: ICouplingLine[]}`. `ICouplingLine`: `{filePathA, filePathB, strength, commitCount, type: STRONG/MODERATE/WEAK}`. STRONG > 0.7, MODERATE > 0.4, WEAK <= 0.4. Тесты типов. Готово, если: для CORE-299 полностью выполнены пункты блока «Реализация», поведение подтверждено unit/integration тестами (happy-path, negative, edge-case), контракты DTO/ports совместимы, регрессии в смежных use case отсутствуют; DoD: `cd packages/core && bun run lint && bun run typecheck && bun test`. |
| CORE-300 | Реализовать healthDegradationOverlay DTO | TODO | Не начато | Реализация: `application/dto/analytics/health-degradation-overlay.dto.ts`. `IHealthDegradationOverlay`: `{degradations: IFileDegradation[]}`. `IFileDegradation`: `{filePath, currentScore, previousScore, delta, trend: TrendDirection, degradationRate}`. Тесты типов. Готово, если: для CORE-300 полностью выполнены пункты блока «Реализация», поведение подтверждено unit/integration тестами (happy-path, negative, edge-case), контракты DTO/ports совместимы, регрессии в смежных use case отсутствуют; DoD: `cd packages/core && bun run lint && bun run typecheck && bun test`. |
| CORE-301 | Реализовать bugPropagationOverlay DTO | TODO | Не начато | Реализация: `application/dto/analytics/bug-propagation-overlay.dto.ts`. `IBugPropagationOverlay`: `{propagations: IBugPropagation[]}`. `IBugPropagation`: `{sourceFile, affectedFiles[], bugType, severity, propagationDepth, rootCauseCommit}`. Тесты типов. Готово, если: для CORE-301 полностью выполнены пункты блока «Реализация», поведение подтверждено unit/integration тестами (happy-path, negative, edge-case), контракты DTO/ports совместимы, регрессии в смежных use case отсутствуют; DoD: `cd packages/core && bun run lint && bun run typecheck && bun test`. |
| CORE-302 | Реализовать causalOverlayData aggregate DTO | TODO | Не начато | Реализация: `application/dto/analytics/causal-overlay-data.dto.ts`. `ICausalOverlayData`: `{repositoryId, temporalCoupling: ITemporalCouplingOverlay, healthDegradation: IHealthDegradationOverlay, bugPropagation: IBugPropagationOverlay, generatedAt}`. Aggregate DTO для фронтенда. Тесты типов. Готово, если: для CORE-302 полностью выполнены пункты блока «Реализация», поведение подтверждено unit/integration тестами (happy-path, negative, edge-case), контракты DTO/ports совместимы, регрессии в смежных use case отсутствуют; DoD: `cd packages/core && bun run lint && bun run typecheck && bun test`. |
| CORE-303 | Реализовать getCausalOverlayDataUseCase | TODO | Не начато | Реализация: IUseCase<{repoId, timeWindowDays?}, ICausalOverlayData, DomainError>. Оркестрирует: AnalyzeTemporalCouplingUseCase → HealthDegradation → BugPropagation. Готово, если: для CORE-303 полностью выполнены пункты блока «Реализация», поведение подтверждено unit/integration тестами (happy-path, negative, edge-case), контракты DTO/ports совместимы, регрессии в смежных use case отсутствуют; DoD: `cd packages/core && bun run lint && bun run typecheck && bun test`. |

---

## v0.55.0 — Developer Onboarding Domain

> DTO, порты и use cases для онбординга нового разработчика: обзор проекта, AI-guided tour.

> **Результат версии:** Завершена версия «v0.55.0 — Developer Onboarding Domain» в рамках M15; инкремент готовит продукт к стабильному end-to-end сценарию в своем слое.

| ID | Задача | Статус | Результат | Acceptance Criteria |
|----------|--------------------|--------|-----------|---------------------|
| CORE-304 | Реализовать projectOverview DTO | TODO | Не начато | Реализация: `application/dto/onboarding/project-overview.dto.ts`. `IProjectOverview`: `{repositoryId, totalFiles, totalLOC, topLanguages: {language, percentage}[], keyModules: {path, description, fileCount}[], entryPoints: {filePath, type: "main"/"config"/"router"/"index", description}[], architectureStyle?, generatedAt}`. Тесты типов. Готово, если: для CORE-304 полностью выполнены пункты блока «Реализация», поведение подтверждено unit/integration тестами (happy-path, negative, edge-case), контракты DTO/ports совместимы, регрессии в смежных use case отсутствуют; DoD: `cd packages/core && bun run lint && bun run typecheck && bun test`. |
| CORE-305 | Реализовать guidedTour DTO | TODO | Не начато | Реализация: `application/dto/onboarding/guided-tour.dto.ts`. `IGuidedTourStep`: `{order, title, description, targetFilePath, highlightLines?, codeSnippet?, explanation, relatedFiles?}`. `IGuidedTour`: `{repositoryId, tourName, steps[], estimatedMinutes, difficulty: "beginner"/"intermediate"/"advanced", generatedAt}`. Тесты типов. Готово, если: для CORE-305 полностью выполнены пункты блока «Реализация», поведение подтверждено unit/integration тестами (happy-path, negative, edge-case), контракты DTO/ports совместимы, регрессии в смежных use case отсутствуют; DoD: `cd packages/core && bun run lint && bun run typecheck && bun test`. |
| CORE-306 | Реализовать getProjectOverviewUseCase | TODO | Не начато | Реализация: IUseCase<{repoId, branch?}, IProjectOverview, DomainError>. Загружает CodeGraph → извлекает file nodes → группирует по directories → определяет entryPoints. Готово, если: для CORE-306 полностью выполнены пункты блока «Реализация», поведение подтверждено unit/integration тестами (happy-path, negative, edge-case), контракты DTO/ports совместимы, регрессии в смежных use case отсутствуют; DoD: `cd packages/core && bun run lint && bun run typecheck && bun test`. |
| CORE-307 | Реализовать generateGuidedTourUseCase | TODO | Не начато | Реализация: IUseCase<{repoId, focus?, difficulty?}, IGuidedTour, DomainError>. Загружает ProjectOverview → формирует prompt → ILLMProvider генерирует steps (5-15). Fallback без LLM: базовый tour из entryPoints + keyModules. Готово, если: для CORE-307 полностью выполнены пункты блока «Реализация», поведение подтверждено unit/integration тестами (happy-path, negative, edge-case), контракты DTO/ports совместимы, регрессии в смежных use case отсутствуют; DoD: `cd packages/core && bun run lint && bun run typecheck && bun test`. |
| CORE-308 | Реализовать ioC-токены для onboarding | TODO | Не начато | Реализация: `infrastructure/ioc/tokens.ts` — добавить `TOKENS.UseCases.GetProjectOverview`, `TOKENS.UseCases.GenerateGuidedTour`. Готово, если: для CORE-308 полностью выполнены пункты блока «Реализация», поведение подтверждено unit/integration тестами (happy-path, negative, edge-case), контракты DTO/ports совместимы, регрессии в смежных use case отсутствуют; DoD: `cd packages/core && bun run lint && bun run typecheck && bun test`. |

---

## v0.56.0 — Refactoring Planning Domain

> DTO, domain service и use cases для планирования рефакторинга: ROI, приоритизация, симуляция.

> **Результат версии:** Завершена версия «v0.56.0 — Refactoring Planning Domain» в рамках M15; инкремент готовит продукт к стабильному end-to-end сценарию в своем слое.

| ID | Задача | Статус | Результат | Acceptance Criteria |
|----------|--------------------|--------|-----------|---------------------|
| CORE-309 | Реализовать refactoringCandidate DTO | TODO | Не начато | Реализация: `application/dto/refactoring/refactoring-candidate.dto.ts`. `IRefactoringCandidate`: `{filePath, reason, category: "complexity"/"duplication"/"coupling"/"size"/"churn"/"debt", estimatedEffortHours, estimatedImpactScore (0-100), roi (impact/effort), currentMetrics: {loc, complexity, churn, issueCount}, affectedDependents}`. Готово, если: для CORE-309 полностью выполнены пункты блока «Реализация», поведение подтверждено unit/integration тестами (happy-path, negative, edge-case), контракты DTO/ports совместимы, регрессии в смежных use case отсутствуют; DoD: `cd packages/core && bun run lint && bun run typecheck && bun test`. |
| CORE-310 | Реализовать refactoringPlan DTO | TODO | Не начато | Реализация: `application/dto/refactoring/refactoring-plan.dto.ts`. `IRefactoringPlan`: `{repositoryId, candidates[], totalEffortHours, expectedImprovementPercent, priorityOrder: string[] (filePaths sorted by ROI desc), generatedAt}`. Тесты типов. Готово, если: для CORE-310 полностью выполнены пункты блока «Реализация», поведение подтверждено unit/integration тестами (happy-path, negative, edge-case), контракты DTO/ports совместимы, регрессии в смежных use case отсутствуют; DoD: `cd packages/core && bun run lint && bun run typecheck && bun test`. |
| CORE-311 | Реализовать refactoringSimulationResult DTO | TODO | Не начато | Реализация: `application/dto/refactoring/refactoring-simulation.dto.ts`. `IRefactoringSimulationResult`: `{targetFilePath, beforeMetrics, afterMetrics, affectedFiles: {filePath, impactType: "direct"/"indirect"}[], riskLevel: "low"/"medium"/"high", explanation}`. Тесты типов. Готово, если: для CORE-311 полностью выполнены пункты блока «Реализация», поведение подтверждено unit/integration тестами (happy-path, negative, edge-case), контракты DTO/ports совместимы, регрессии в смежных use case отсутствуют; DoD: `cd packages/core && bun run lint && bun run typecheck && bun test`. |
| CORE-312 | Реализовать refactoringScorer domain service | TODO | Не начато | Реализация: `domain/services/refactoring-scorer.service.ts`. `scoreCandidate(metrics, dependentCount): {estimatedEffort, estimatedImpact, roi}`. `prioritize(candidates[]): IRefactoringCandidate[]` — сортировка по ROI desc. Готово, если: для CORE-312 полностью выполнены пункты блока «Реализация», поведение подтверждено unit/integration тестами (happy-path, negative, edge-case), контракты DTO/ports совместимы, регрессии в смежных use case отсутствуют; DoD: `cd packages/core && bun run lint && bun run typecheck && bun test`. |
| CORE-313 | Реализовать prioritizeRefactoringUseCase | TODO | Не начато | Реализация: IUseCase<{repoId, maxCandidates?, minRoi?}, IRefactoringPlan, DomainError>. Загружает CodeGraph → FileMetrics → RefactoringScorer → фильтрует по minRoi → ограничивает maxCandidates (default 20). Готово, если: для CORE-313 полностью выполнены пункты блока «Реализация», поведение подтверждено unit/integration тестами (happy-path, negative, edge-case), контракты DTO/ports совместимы, регрессии в смежных use case отсутствуют; DoD: `cd packages/core && bun run lint && bun run typecheck && bun test`. |
| CORE-314 | Реализовать simulateRefactoringUseCase | TODO | Не начато | Реализация: IUseCase<{repoId, targetFilePath, refactoringType: "split"/"extract"/"simplify"/"decouple"}, IRefactoringSimulationResult, DomainError>. Загружает метрики → IImpactAnalyzer → ILLMProvider для explanation и прогноза afterMetrics. Готово, если: для CORE-314 полностью выполнены пункты блока «Реализация», поведение подтверждено unit/integration тестами (happy-path, negative, edge-case), контракты DTO/ports совместимы, регрессии в смежных use case отсутствуют; DoD: `cd packages/core && bun run lint && bun run typecheck && bun test`. |
| CORE-315 | Реализовать ioC-токены для refactoring | TODO | Не начато | Реализация: Добавить `TOKENS.UseCases.PrioritizeRefactoring`, `TOKENS.UseCases.SimulateRefactoring`. Готово, если: для CORE-315 полностью выполнены пункты блока «Реализация», поведение подтверждено unit/integration тестами (happy-path, negative, edge-case), контракты DTO/ports совместимы, регрессии в смежных use case отсутствуют; DoD: `cd packages/core && bun run lint && bun run typecheck && bun test`. |

---

## v0.57.0 — Impact Planning Domain

> DTO и use case для pre-code impact simulation: "что затронется если я поменяю файл X".

> **Результат версии:** Завершена версия «v0.57.0 — Impact Planning Domain» в рамках M15; инкремент готовит продукт к стабильному end-to-end сценарию в своем слое.

| ID | Задача | Статус | Результат | Acceptance Criteria |
|----------|--------------------|--------|-----------|---------------------|
| CORE-316 | Реализовать impactSimulation DTO | TODO | Не начато | Реализация: `application/dto/impact/impact-simulation.dto.ts`. `IImpactSimulation`: `{targetFiles[], affectedFiles: {filePath, impactType: "direct"/"transitive", depth}[], affectedTests: {filePath, testName?}[], riskLevel: "low"/"medium"/"high"/"critical", blastRadius, estimatedReviewComplexity: "simple"/"moderate"/"complex", explanation}`. Готово, если: для CORE-316 полностью выполнены пункты блока «Реализация», поведение подтверждено unit/integration тестами (happy-path, negative, edge-case), контракты DTO/ports совместимы, регрессии в смежных use case отсутствуют; DoD: `cd packages/core && bun run lint && bun run typecheck && bun test`. |
| CORE-317 | Реализовать impactReport DTO | TODO | Не начато | Реализация: `application/dto/impact/impact-report.dto.ts`. `IImpactReport`: `{repositoryId, simulation: IImpactSimulation, recommendations[], suggestedReviewers[], generatedAt}`. Тесты типов. Готово, если: для CORE-317 полностью выполнены пункты блока «Реализация», поведение подтверждено unit/integration тестами (happy-path, negative, edge-case), контракты DTO/ports совместимы, регрессии в смежных use case отсутствуют; DoD: `cd packages/core && bun run lint && bun run typecheck && bun test`. |
| CORE-318 | Реализовать blastRadius domain service | TODO | Не начато | Реализация: `domain/services/blast-radius.service.ts`. `calculate(directCount, transitiveCount, totalFiles): {blastRadius, riskLevel}`. Thresholds: low <5%, medium <15%, high <30%, critical >=30%. `estimateReviewComplexity(affectedCount, maxDepth)`. Готово, если: для CORE-318 полностью выполнены пункты блока «Реализация», поведение подтверждено unit/integration тестами (happy-path, negative, edge-case), контракты DTO/ports совместимы, регрессии в смежных use case отсутствуют; DoD: `cd packages/core && bun run lint && bun run typecheck && bun test`. |
| CORE-319 | Реализовать simulateChangeImpactUseCase | TODO | Не начато | Реализация: IUseCase<{repoId, targetFiles[]}, IImpactReport, DomainError>. CodeGraph → IImpactAnalyzer → фильтрует тесты → BlastRadiusService → ILLMProvider для recommendations (optional, fallback). Готово, если: для CORE-319 полностью выполнены пункты блока «Реализация», поведение подтверждено unit/integration тестами (happy-path, negative, edge-case), контракты DTO/ports совместимы, регрессии в смежных use case отсутствуют; DoD: `cd packages/core && bun run lint && bun run typecheck && bun test`. |
| CORE-320 | Реализовать ioC-токены для impact planning | TODO | Не начато | Реализация: Добавить `TOKENS.UseCases.SimulateChangeImpact`. Готово, если: для CORE-320 полностью выполнены пункты блока «Реализация», поведение подтверждено unit/integration тестами (happy-path, negative, edge-case), контракты DTO/ports совместимы, регрессии в смежных use case отсутствуют; DoD: `cd packages/core && bun run lint && bun run typecheck && bun test`. |

---

## v0.58.0 — Knowledge Map & Bus Factor Domain

> DTO, порты и use cases для knowledge map: ownership, bus factor, knowledge silos.

> **Результат версии:** Завершена версия «v0.58.0 — Knowledge Map & Bus Factor Domain» в рамках M15; инкремент готовит продукт к стабильному end-to-end сценарию в своем слое.

| ID | Задача | Статус | Результат | Acceptance Criteria |
|----------|--------------------|--------|-----------|---------------------|
| CORE-321 | Реализовать fileOwnership DTO | TODO | Не начато | Реализация: `application/dto/knowledge/file-ownership.dto.ts`. `IFileOwnership`: `{filePath, primaryOwner, contributors: {name, commitCount, lastCommitDate, linesChanged}[], lastModifiedBy, lastModifiedDate, busFactor}`. Тесты типов. Готово, если: для CORE-321 полностью выполнены пункты блока «Реализация», поведение подтверждено unit/integration тестами (happy-path, negative, edge-case), контракты DTO/ports совместимы, регрессии в смежных use case отсутствуют; DoD: `cd packages/core && bun run lint && bun run typecheck && bun test`. |
| CORE-322 | Реализовать knowledgeMap DTO | TODO | Не начато | Реализация: `application/dto/knowledge/knowledge-map.dto.ts`. `IKnowledgeMap`: `{repositoryId, ownership: IFileOwnership[], silos: {filePath, owner, risk: "low"/"medium"/"high"/"critical"}[], experts: {name, domains[], filesOwned}[], overallBusFactor, generatedAt}`. Тесты типов. Готово, если: для CORE-322 полностью выполнены пункты блока «Реализация», поведение подтверждено unit/integration тестами (happy-path, negative, edge-case), контракты DTO/ports совместимы, регрессии в смежных use case отсутствуют; DoD: `cd packages/core && bun run lint && bun run typecheck && bun test`. |
| CORE-323 | Реализовать teamKnowledge DTO | TODO | Не начато | Реализация: `application/dto/knowledge/team-knowledge.dto.ts`. `ITeamKnowledge`: `{teamId, teamName, coveredFiles[], gaps: {filePath, reason: "no_team_contributor"/"single_contributor"/"stale_knowledge"}[], coveragePercent, memberExpertise: {memberId, name, expertise[]}[]}`. Тесты типов. Готово, если: для CORE-323 полностью выполнены пункты блока «Реализация», поведение подтверждено unit/integration тестами (happy-path, negative, edge-case), контракты DTO/ports совместимы, регрессии в смежных use case отсутствуют; DoD: `cd packages/core && bun run lint && bun run typecheck && bun test`. |
| CORE-324 | Реализовать iOwnershipProvider порт | TODO | Не начато | Реализация: `application/ports/outbound/knowledge/ownership-provider.ts`. `getFileOwnership(repoId, filePaths[]): Promise<IFileOwnership[]>`, `getContributors(repoId): Promise<{name, email, commitCount}[]>`. Токен `TOKENS.Analysis.OwnershipProvider`. Готово, если: для CORE-324 полностью выполнены пункты блока «Реализация», поведение подтверждено unit/integration тестами (happy-path, negative, edge-case), контракты DTO/ports совместимы, регрессии в смежных use case отсутствуют; DoD: `cd packages/core && bun run lint && bun run typecheck && bun test`. |
| CORE-325 | Реализовать busFactorCalculator service | TODO | Не начато | Реализация: `domain/services/bus-factor-calculator.service.ts`. `calculateFileBusFactor(contributors[])` — кол-во контрибьюторов покрывающих 80% коммитов. `calculateOverallBusFactor(ownership[])` — медиана. `detectSilos(ownership[])` — silo если busFactor === 1 и commits > 10. Готово, если: для CORE-325 полностью выполнены пункты блока «Реализация», поведение подтверждено unit/integration тестами (happy-path, negative, edge-case), контракты DTO/ports совместимы, регрессии в смежных use case отсутствуют; DoD: `cd packages/core && bun run lint && bun run typecheck && bun test`. |
| CORE-326 | Реализовать calculateBusFactorUseCase | TODO | Не начато | Реализация: IUseCase<{repoId}, {overall, files: {filePath, busFactor}[]}, DomainError>. CodeGraph → IOwnershipProvider → BusFactorCalculator. Готово, если: для CORE-326 полностью выполнены пункты блока «Реализация», поведение подтверждено unit/integration тестами (happy-path, negative, edge-case), контракты DTO/ports совместимы, регрессии в смежных use case отсутствуют; DoD: `cd packages/core && bun run lint && bun run typecheck && bun test`. |
| CORE-327 | Реализовать getKnowledgeMapUseCase | TODO | Не начато | Реализация: IUseCase<{repoId, teamId?}, IKnowledgeMap, DomainError>. Ownership → группировка experts → detectSilos → если teamId — фильтрация по Team entity. Готово, если: для CORE-327 полностью выполнены пункты блока «Реализация», поведение подтверждено unit/integration тестами (happy-path, negative, edge-case), контракты DTO/ports совместимы, регрессии в смежных use case отсутствуют; DoD: `cd packages/core && bun run lint && bun run typecheck && bun test`. |
| CORE-328 | Реализовать detectKnowledgeSilosUseCase | TODO | Не начато | Реализация: IUseCase<{repoId, riskThreshold?}, {silos[], totalSilos, criticalSilos}, DomainError>. Вызывает GetKnowledgeMapUseCase → фильтрует по riskThreshold (default "medium"). Готово, если: для CORE-328 полностью выполнены пункты блока «Реализация», поведение подтверждено unit/integration тестами (happy-path, negative, edge-case), контракты DTO/ports совместимы, регрессии в смежных use case отсутствуют; DoD: `cd packages/core && bun run lint && bun run typecheck && bun test`. |
| CORE-329 | Реализовать ioC-токены для knowledge | TODO | Не начато | Реализация: Добавить `TOKENS.Analysis.OwnershipProvider`, `TOKENS.UseCases.CalculateBusFactor`, `TOKENS.UseCases.GetKnowledgeMap`, `TOKENS.UseCases.DetectKnowledgeSilos`. Готово, если: для CORE-329 полностью выполнены пункты блока «Реализация», поведение подтверждено unit/integration тестами (happy-path, negative, edge-case), контракты DTO/ports совместимы, регрессии в смежных use case отсутствуют; DoD: `cd packages/core && bun run lint && bun run typecheck && bun test`. |

---

## v0.59.0 — AI Predictive Analytics Domain

> DTO, порты, domain service и use cases для AI-прогнозирования hotspots и рисков. Гибрид: статистика + LLM.

> **Результат версии:** Завершена версия «v0.59.0 — AI Predictive Analytics Domain» в рамках M15; инкремент готовит продукт к стабильному end-to-end сценарию в своем слое.

| ID | Задача | Статус | Результат | Acceptance Criteria |
|----------|--------------------|--------|-----------|---------------------|
| CORE-330 | Реализовать predictiveHotspot DTO | TODO | Не начато | Реализация: `application/dto/prediction/predictive-hotspot.dto.ts`. `IPredictiveHotspot`: `{filePath, predictedRisk (0-100), confidence (0-1), factors: {name, weight, value, description}[], trend: "increasing"/"stable"/"decreasing", explanation?}`. Тесты типов. Готово, если: для CORE-330 полностью выполнены пункты блока «Реализация», поведение подтверждено unit/integration тестами (happy-path, negative, edge-case), контракты DTO/ports совместимы, регрессии в смежных use case отсутствуют; DoD: `cd packages/core && bun run lint && bun run typecheck && bun test`. |
| CORE-331 | Реализовать predictionResult DTO | TODO | Не начато | Реализация: `application/dto/prediction/prediction-result.dto.ts`. `IPredictionResult`: `{repositoryId, hotspots: IPredictiveHotspot[], modelVersion, dataPointsUsed, generatedAt, expiresAt}`. Тесты типов. Готово, если: для CORE-331 полностью выполнены пункты блока «Реализация», поведение подтверждено unit/integration тестами (happy-path, negative, edge-case), контракты DTO/ports совместимы, регрессии в смежных use case отсутствуют; DoD: `cd packages/core && bun run lint && bun run typecheck && bun test`. |
| CORE-332 | Реализовать iPredictionService порт | TODO | Не начато | Реализация: `application/ports/outbound/analysis/prediction-service.ts`. `predictHotspots(repoId, filePaths[]): Promise<IPredictiveHotspot[]>`, `predictRegressions(repoId): Promise<{filePath, probability, reason}[]>`. Токен `TOKENS.Analysis.PredictionService`. Готово, если: для CORE-332 полностью выполнены пункты блока «Реализация», поведение подтверждено unit/integration тестами (happy-path, negative, edge-case), контракты DTO/ports совместимы, регрессии в смежных use case отсутствуют; DoD: `cd packages/core && bun run lint && bun run typecheck && bun test`. |
| CORE-333 | Реализовать trendAnalysis domain service | TODO | Не начато | Реализация: `domain/services/trend-analysis.service.ts`. `calculateChurnVelocity(churnHistory[])` — линейная регрессия. `calculateComplexityAcceleration(complexityHistory[])`. `calculateDefectDensity(issueCount, loc)`. `compositePrediction(churnVelocity, complexityRate, defectDensity, busFactor): {predictedRisk, confidence, factors[]}`. Готово, если: для CORE-333 полностью выполнены пункты блока «Реализация», поведение подтверждено unit/integration тестами (happy-path, negative, edge-case), контракты DTO/ports совместимы, регрессии в смежных use case отсутствуют; DoD: `cd packages/core && bun run lint && bun run typecheck && bun test`. |
| CORE-334 | Реализовать predictHotspotsUseCase | TODO | Не начато | Реализация: IUseCase<{repoId, limit?}, IPredictionResult, DomainError>. CodeGraph → FileMetrics → IOwnershipProvider → TrendAnalysisService.compositePrediction() per file → sort by risk desc → limit (default 50). Готово, если: для CORE-334 полностью выполнены пункты блока «Реализация», поведение подтверждено unit/integration тестами (happy-path, negative, edge-case), контракты DTO/ports совместимы, регрессии в смежных use case отсутствуют; DoD: `cd packages/core && bun run lint && bun run typecheck && bun test`. |
| CORE-335 | Реализовать explainPredictionUseCase | TODO | Не начато | Реализация: IUseCase<{repoId, filePath}, {filePath, explanation, factors[], recommendations[]}, DomainError>. PredictHotspots для файла → ILLMProvider.chat() для explanation. Fallback: шаблонное explanation. Готово, если: для CORE-335 полностью выполнены пункты блока «Реализация», поведение подтверждено unit/integration тестами (happy-path, negative, edge-case), контракты DTO/ports совместимы, регрессии в смежных use case отсутствуют; DoD: `cd packages/core && bun run lint && bun run typecheck && bun test`. |
| CORE-336 | Реализовать ioC-токены для prediction | TODO | Не начато | Реализация: Добавить `TOKENS.Analysis.PredictionService`, `TOKENS.UseCases.PredictHotspots`, `TOKENS.UseCases.ExplainPrediction`. Готово, если: для CORE-336 полностью выполнены пункты блока «Реализация», поведение подтверждено unit/integration тестами (happy-path, negative, edge-case), контракты DTO/ports совместимы, регрессии в смежных use case отсутствуют; DoD: `cd packages/core && bun run lint && bun run typecheck && bun test`. |

---

## v0.60.0 — Sprint Gamification Domain

> DTO, порты и use cases для sprint snapshot и district trend indicators. Lightweight gamification.

> **Результат версии:** Завершена версия «v0.60.0 — Sprint Gamification Domain» в рамках M15; инкремент готовит продукт к стабильному end-to-end сценарию в своем слое.

| ID | Задача | Статус | Результат | Acceptance Criteria |
|----------|--------------------|--------|-----------|---------------------|
| CORE-337 | Реализовать sprintSnapshot DTO | TODO | Не начато | Реализация: `application/dto/analytics/sprint-snapshot.dto.ts`. `ISprintSnapshot`: `{repositoryId, sprintId, snapshotDate, metricsPerDistrict: IDistrictMetricsSnapshot[]}`. `IDistrictMetricsSnapshot`: `{districtPath, avgComplexity, totalLoc, issueCount, coverageAvg?, churnTotal, healthScore (0-100)}`. Тесты типов. Готово, если: для CORE-337 полностью выполнены пункты блока «Реализация», поведение подтверждено unit/integration тестами (happy-path, negative, edge-case), контракты DTO/ports совместимы, регрессии в смежных use case отсутствуют; DoD: `cd packages/core && bun run lint && bun run typecheck && bun test`. |
| CORE-338 | Реализовать districtTrend DTO | TODO | Не начато | Реализация: `application/dto/analytics/district-trend.dto.ts`. `IDistrictTrend`: `{districtPath, healthBefore, healthAfter, delta, direction: TrendDirection}`. delta > 5 = improving, < -5 = degrading, else stable. `ISprintComparison`: `{repositoryId, beforeSprintId, afterSprintId, trends[], overallDelta, overallDirection}`. Готово, если: для CORE-338 полностью выполнены пункты блока «Реализация», поведение подтверждено unit/integration тестами (happy-path, negative, edge-case), контракты DTO/ports совместимы, регрессии в смежных use case отсутствуют; DoD: `cd packages/core && bun run lint && bun run typecheck && bun test`. |
| CORE-339 | Реализовать iSprintSnapshotRepository порт | TODO | Не начато | Реализация: `application/ports/outbound/analytics/sprint-snapshot-repository.ts`. `save, findBySprintId(repoId, sprintId), findLatest(repoId, limit), findByDateRange(repoId, from, to)`. Токен `TOKENS.Analysis.SprintSnapshotRepository`. Готово, если: для CORE-339 полностью выполнены пункты блока «Реализация», поведение подтверждено unit/integration тестами (happy-path, negative, edge-case), контракты DTO/ports совместимы, регрессии в смежных use case отсутствуют; DoD: `cd packages/core && bun run lint && bun run typecheck && bun test`. |
| CORE-340 | Реализовать districtHealthCalculator service | TODO | Не начато | Реализация: `domain/services/district-health-calculator.service.ts`. `calculateHealth(metrics): number` — weights: complexity(0.3), coverage(0.25), issueCount(0.25), churn(0.2). `compareTrend(before, after, threshold?): TrendDirection`. Готово, если: для CORE-340 полностью выполнены пункты блока «Реализация», поведение подтверждено unit/integration тестами (happy-path, negative, edge-case), контракты DTO/ports совместимы, регрессии в смежных use case отсутствуют; DoD: `cd packages/core && bun run lint && bun run typecheck && bun test`. |
| CORE-341 | Реализовать takeSprintSnapshotUseCase | TODO | Не начато | Реализация: IUseCase<{repositoryId, sprintId}, ISprintSnapshot, DomainError>. GetCodeCityDataUseCase → извлекает directories → DistrictHealthCalculator → сохраняет через ISprintSnapshotRepository. Готово, если: для CORE-341 полностью выполнены пункты блока «Реализация», поведение подтверждено unit/integration тестами (happy-path, negative, edge-case), контракты DTO/ports совместимы, регрессии в смежных use case отсутствуют; DoD: `cd packages/core && bun run lint && bun run typecheck && bun test`. |
| CORE-342 | Реализовать compareSprintsUseCase | TODO | Не начато | Реализация: IUseCase<{repositoryId, beforeSprintId, afterSprintId}, ISprintComparison, DomainError>. Загружает оба snapshot → матчит districts → delta/direction через DistrictHealthCalculator. Готово, если: для CORE-342 полностью выполнены пункты блока «Реализация», поведение подтверждено unit/integration тестами (happy-path, negative, edge-case), контракты DTO/ports совместимы, регрессии в смежных use case отсутствуют; DoD: `cd packages/core && bun run lint && bun run typecheck && bun test`. |
| CORE-343 | Реализовать getDistrictTrendsUseCase | TODO | Не начато | Реализация: IUseCase<{repositoryId, sprintCount?}, {trends[], sprintIds[]}, DomainError>. Загружает последние N snapshots → trend per district. Готово, если: для CORE-343 полностью выполнены пункты блока «Реализация», поведение подтверждено unit/integration тестами (happy-path, negative, edge-case), контракты DTO/ports совместимы, регрессии в смежных use case отсутствуют; DoD: `cd packages/core && bun run lint && bun run typecheck && bun test`. |
| CORE-344 | Реализовать tOKENS для Sprint Gamification | TODO | Не начато | Реализация: Добавить `TOKENS.Analysis.SprintSnapshotRepository`, `TOKENS.UseCases.TakeSprintSnapshot`, `TOKENS.UseCases.CompareSprints`, `TOKENS.UseCases.GetDistrictTrends`. Готово, если: для CORE-344 полностью выполнены пункты блока «Реализация», поведение подтверждено unit/integration тестами (happy-path, negative, edge-case), контракты DTO/ports совместимы, регрессии в смежных use case отсутствуют; DoD: `cd packages/core && bun run lint && bun run typecheck && bun test`. |

---

## v0.61.0 — Architecture Drift Domain

> Blueprint, drift detection, tracking over time.

> **Результат версии:** Завершена версия «v0.61.0 — Architecture Drift Domain» в рамках M15; инкремент готовит продукт к стабильному end-to-end сценарию в своем слое.

| ID | Задача | Статус | Результат | Acceptance Criteria |
|----------|--------------------|--------|-----------|---------------------|
| CORE-345 | Реализовать architectureBlueprint DTO | TODO | Не начато | Реализация: `application/dto/architecture/architecture-blueprint.dto.ts`. `IArchitectureBlueprint`: `{repositoryId, name, layers: IBlueprintLayer[], allowedDependencies: IDependencyRule[], forbiddenDependencies: IDependencyRule[], createdAt, updatedAt}`. `IBlueprintLayer`: `{name, pathPatterns[], description?}`. `IDependencyRule`: `{fromLayer, toLayer, description?}`. Готово, если: для CORE-345 полностью выполнены пункты блока «Реализация», поведение подтверждено unit/integration тестами (happy-path, negative, edge-case), контракты DTO/ports совместимы, регрессии в смежных use case отсутствуют; DoD: `cd packages/core && bun run lint && bun run typecheck && bun test`. |
| CORE-346 | Реализовать architectureDrift DTO | TODO | Не начато | Реализация: `IArchitectureDrift`: `{repositoryId, blueprintName, violations: ILayerViolation[], driftScore (0-100), newViolationsSinceLastScan, totalFiles, compliantFiles, scanDate}`. `IDriftOverTime`: `{repositoryId, dataPoints: IDriftDataPoint[]}`. `IDriftDataPoint`: `{date, driftScore, violationCount, newViolations, resolvedViolations}`. Готово, если: для CORE-346 полностью выполнены пункты блока «Реализация», поведение подтверждено unit/integration тестами (happy-path, negative, edge-case), контракты DTO/ports совместимы, регрессии в смежных use case отсутствуют; DoD: `cd packages/core && bun run lint && bun run typecheck && bun test`. |
| CORE-347 | Реализовать iBlueprintRepository порт | TODO | Не начато | Реализация: `save, findByRepoAndName(repoId, name), findByRepo(repoId), delete(repoId, name)`. Токен `TOKENS.Analysis.BlueprintRepository`. Готово, если: для CORE-347 полностью выполнены пункты блока «Реализация», поведение подтверждено unit/integration тестами (happy-path, negative, edge-case), контракты DTO/ports совместимы, регрессии в смежных use case отсутствуют; DoD: `cd packages/core && bun run lint && bun run typecheck && bun test`. |
| CORE-348 | Реализовать iDriftScanRepository порт | TODO | Не начато | Реализация: `saveScan, findLatest(repoId), findHistory(repoId, limit), findByDateRange(repoId, from, to)`. Токен `TOKENS.Analysis.DriftScanRepository`. Готово, если: для CORE-348 полностью выполнены пункты блока «Реализация», поведение подтверждено unit/integration тестами (happy-path, negative, edge-case), контракты DTO/ports совместимы, регрессии в смежных use case отсутствуют; DoD: `cd packages/core && bun run lint && bun run typecheck && bun test`. |
| CORE-349 | Реализовать driftScoreCalculator service | TODO | Не начато | Реализация: `domain/services/drift-score-calculator.service.ts`. `calculateDriftScore(totalFiles, violatedFiles): number` (0-100). `calculateNewViolations(current[], previous[])`. `calculateResolvedViolations(current[], previous[])`. Готово, если: для CORE-349 полностью выполнены пункты блока «Реализация», поведение подтверждено unit/integration тестами (happy-path, negative, edge-case), контракты DTO/ports совместимы, регрессии в смежных use case отсутствуют; DoD: `cd packages/core && bun run lint && bun run typecheck && bun test`. |
| CORE-350 | Реализовать defineArchitectureBlueprintUseCase | TODO | Не начато | Реализация: IUseCase<{repositoryId, name, layers[], allowed[], forbidden[]}, IArchitectureBlueprint, DomainError>. Validation: min 2 layers, нет self-reference, все layer references существуют. Готово, если: для CORE-350 полностью выполнены пункты блока «Реализация», поведение подтверждено unit/integration тестами (happy-path, negative, edge-case), контракты DTO/ports совместимы, регрессии в смежных use case отсутствуют; DoD: `cd packages/core && bun run lint && bun run typecheck && bun test`. |
| CORE-351 | Реализовать detectArchitectureDriftUseCase | TODO | Не начато | Реализация: IUseCase<{repositoryId, blueprintName?}, IArchitectureDrift, DomainError>. Загружает blueprint → IArchitectureAnalyzer.detectViolations() → фильтрует по rules → DriftScoreCalculator → сравнение с предыдущим scan. Готово, если: для CORE-351 полностью выполнены пункты блока «Реализация», поведение подтверждено unit/integration тестами (happy-path, negative, edge-case), контракты DTO/ports совместимы, регрессии в смежных use case отсутствуют; DoD: `cd packages/core && bun run lint && bun run typecheck && bun test`. |
| CORE-352 | Реализовать trackDriftOverTimeUseCase | TODO | Не начато | Реализация: IUseCase<{repositoryId, fromDate?, toDate?, limit?}, IDriftOverTime, DomainError>. Загружает историю drift scans → преобразует в dataPoints. Готово, если: для CORE-352 полностью выполнены пункты блока «Реализация», поведение подтверждено unit/integration тестами (happy-path, negative, edge-case), контракты DTO/ports совместимы, регрессии в смежных use case отсутствуют; DoD: `cd packages/core && bun run lint && bun run typecheck && bun test`. |
| CORE-353 | Реализовать tOKENS для Architecture Drift | TODO | Не начато | Реализация: Добавить `TOKENS.Analysis.BlueprintRepository`, `TOKENS.Analysis.DriftScanRepository`, `TOKENS.UseCases.DefineArchitectureBlueprint`, `TOKENS.UseCases.DetectArchitectureDrift`, `TOKENS.UseCases.TrackDriftOverTime`. Готово, если: для CORE-353 полностью выполнены пункты блока «Реализация», поведение подтверждено unit/integration тестами (happy-path, negative, edge-case), контракты DTO/ports совместимы, регрессии в смежных use case отсутствуют; DoD: `cd packages/core && bun run lint && bun run typecheck && bun test`. |

---

## v0.62.0 — Executive Reports Domain

> Генерация executive reports (AI-summary), экспорт (PDF/PNG/HTML), scheduled delivery.

> **Результат версии:** Завершена версия «v0.62.0 — Executive Reports Domain» в рамках M15; инкремент готовит продукт к стабильному end-to-end сценарию в своем слое.

| ID | Задача | Статус | Результат | Acceptance Criteria |
|----------|--------------------|--------|-----------|---------------------|
| CORE-354 | Реализовать executiveReport DTO | TODO | Не начато | Реализация: `application/dto/reports/executive-report.dto.ts`. `IExecutiveReport`: `{reportId, repositoryId, period: IReportPeriod, generatedAt, summary, topHotspots: IReportHotspot[], trendDirection, overallHealthScore, recommendations[], metrics: IReportMetrics}`. IReportPeriod: `{from, to, label?}`. IReportHotspot/IReportMetrics. Готово, если: для CORE-354 полностью выполнены пункты блока «Реализация», поведение подтверждено unit/integration тестами (happy-path, negative, edge-case), контракты DTO/ports совместимы, регрессии в смежных use case отсутствуют; DoD: `cd packages/core && bun run lint && bun run typecheck && bun test`. |
| CORE-355 | Реализовать iReportExporter порт | TODO | Не начато | Реализация: ReportFormat: PDF/PNG/HTML. `export(report, format): Promise<IExportedReport>`. `IExportedReport`: `{reportId, format, content: Buffer, mimeType, fileName, generatedAt}`. Токен `TOKENS.Reports.ReportExporter`. Готово, если: для CORE-355 полностью выполнены пункты блока «Реализация», поведение подтверждено unit/integration тестами (happy-path, negative, edge-case), контракты DTO/ports совместимы, регрессии в смежных use case отсутствуют; DoD: `cd packages/core && bun run lint && bun run typecheck && bun test`. |
| CORE-356 | Реализовать iReportRepository порт | TODO | Не начато | Реализация: `save, findById(reportId), findByRepo(repoId, limit?), findByDateRange(repoId, from, to)`. Токен `TOKENS.Reports.ReportRepository`. Готово, если: для CORE-356 полностью выполнены пункты блока «Реализация», поведение подтверждено unit/integration тестами (happy-path, negative, edge-case), контракты DTO/ports совместимы, регрессии в смежных use case отсутствуют; DoD: `cd packages/core && bun run lint && bun run typecheck && bun test`. |
| CORE-357 | Реализовать iReportScheduleRepository порт | TODO | Не начато | Реализация: `IReportSchedule`: `{scheduleId, repositoryId, cronExpression, format, recipients[], channels[], enabled, lastRunAt?, nextRunAt?}`. `save, findByRepo, findById, findDue(now), delete`. Токен `TOKENS.Reports.ReportScheduleRepository`. Готово, если: для CORE-357 полностью выполнены пункты блока «Реализация», поведение подтверждено unit/integration тестами (happy-path, negative, edge-case), контракты DTO/ports совместимы, регрессии в смежных use case отсутствуют; DoD: `cd packages/core && bun run lint && bun run typecheck && bun test`. |
| CORE-358 | Реализовать reportSummaryGenerator service | TODO | Не начато | Реализация: `domain/services/report-summary-generator.service.ts`. `buildSummaryPrompt(metrics, hotspots[], trend): string`. `extractRecommendations(summary): string[]`. Готово, если: для CORE-358 полностью выполнены пункты блока «Реализация», поведение подтверждено unit/integration тестами (happy-path, negative, edge-case), контракты DTO/ports совместимы, регрессии в смежных use case отсутствуют; DoD: `cd packages/core && bun run lint && bun run typecheck && bun test`. |
| CORE-359 | Реализовать generateExecutiveReportUseCase | TODO | Не начато | Реализация: IUseCase<{repositoryId, period, format?}, IExecutiveReport, DomainError>. CodeCityData → aggregate metrics → top hotspots → trend из snapshots → LLM summary → сохраняет report. Готово, если: для CORE-359 полностью выполнены пункты блока «Реализация», поведение подтверждено unit/integration тестами (happy-path, negative, edge-case), контракты DTO/ports совместимы, регрессии в смежных use case отсутствуют; DoD: `cd packages/core && bun run lint && bun run typecheck && bun test`. |
| CORE-360 | Реализовать exportReportUseCase | TODO | Не начато | Реализация: IUseCase<{reportId, format}, IExportedReport, DomainError>. Загружает report → IReportExporter.export(). Готово, если: для CORE-360 полностью выполнены пункты блока «Реализация», поведение подтверждено unit/integration тестами (happy-path, negative, edge-case), контракты DTO/ports совместимы, регрессии в смежных use case отсутствуют; DoD: `cd packages/core && bun run lint && bun run typecheck && bun test`. |
| CORE-361 | Реализовать tOKENS для Reports | TODO | Не начато | Реализация: Создать секцию `Reports` в TOKENS: ReportExporter, ReportRepository, ReportScheduleRepository, GenerateExecutiveReport, ExportReport. Готово, если: для CORE-361 полностью выполнены пункты блока «Реализация», поведение подтверждено unit/integration тестами (happy-path, negative, edge-case), контракты DTO/ports совместимы, регрессии в смежных use case отсутствуют; DoD: `cd packages/core && bun run lint && bun run typecheck && bun test`. |

---

## v0.63.0 — Review Context Domain

> Контекстное обогащение code review через CodeCity данные.

> **Результат версии:** Завершена версия «v0.63.0 — Review Context Domain» в рамках M15; инкремент готовит продукт к стабильному end-to-end сценарию в своем слое.

| ID | Задача | Статус | Результат | Acceptance Criteria |
|----------|--------------------|--------|-----------|---------------------|
| CORE-362 | Реализовать reviewContext DTO | TODO | Не начато | Реализация: `application/dto/review/review-context.dto.ts`. `IReviewContext`: `{ccrId, repositoryId, affectedDistricts: IAffectedDistrict[], districtExperts: IDistrictExpert[], riskAssessment: IRiskAssessment, contextualWarnings[]}`. IAffectedDistrict: `{districtPath, filesChanged, totalFiles, healthScore, trend}`. Готово, если: для CORE-362 полностью выполнены пункты блока «Реализация», поведение подтверждено unit/integration тестами (happy-path, negative, edge-case), контракты DTO/ports совместимы, регрессии в смежных use case отсутствуют; DoD: `cd packages/core && bun run lint && bun run typecheck && bun test`. |
| CORE-363 | Реализовать suggestedReviewers DTO | TODO | Не начато | Реализация: `application/dto/review/suggested-reviewers.dto.ts`. `ISuggestedReviewers`: `{ccrId, reviewers: ISuggestedReviewer[]}`. `ISuggestedReviewer`: `{userId, username, score (0-100), reasons[], expertise[]}`. Тесты типов. Готово, если: для CORE-363 полностью выполнены пункты блока «Реализация», поведение подтверждено unit/integration тестами (happy-path, negative, edge-case), контракты DTO/ports совместимы, регрессии в смежных use case отсутствуют; DoD: `cd packages/core && bun run lint && bun run typecheck && bun test`. |
| CORE-364 | Реализовать iKnowledgeMapProvider порт | TODO | Не начато | Реализация: `application/ports/outbound/analysis/knowledge-map-provider.ts`. `getFileExperts(repoId, filePaths[])`, `getDistrictExperts(repoId, districtPaths[])`. Токен `TOKENS.Analysis.KnowledgeMapProvider`. Готово, если: для CORE-364 полностью выполнены пункты блока «Реализация», поведение подтверждено unit/integration тестами (happy-path, negative, edge-case), контракты DTO/ports совместимы, регрессии в смежных use case отсутствуют; DoD: `cd packages/core && bun run lint && bun run typecheck && bun test`. |
| CORE-365 | Реализовать reviewRiskCalculator service | TODO | Не начато | Реализация: `domain/services/review-risk-calculator.service.ts`. `assessRisk({affectedDistricts[], hotspotFilesAffected, complexityDelta, architectureDriftImpact}): IRiskAssessment`. critical если hotspots > 5 или driftImpact, high если avgHealth < 40. Готово, если: для CORE-365 полностью выполнены пункты блока «Реализация», поведение подтверждено unit/integration тестами (happy-path, negative, edge-case), контракты DTO/ports совместимы, регрессии в смежных use case отсутствуют; DoD: `cd packages/core && bun run lint && bun run typecheck && bun test`. |
| CORE-366 | Реализовать reviewerScorer service | TODO | Не начато | Реализация: `domain/services/reviewer-scorer.service.ts`. `scoreReviewers(experts[], maxReviewers): ISuggestedReviewer[]`. Scoring: commitCount(0.5) + recency(0.3) + coverage(0.2). Дедупликация по userId. Готово, если: для CORE-366 полностью выполнены пункты блока «Реализация», поведение подтверждено unit/integration тестами (happy-path, negative, edge-case), контракты DTO/ports совместимы, регрессии в смежных use case отсутствуют; DoD: `cd packages/core && bun run lint && bun run typecheck && bun test`. |
| CORE-367 | Реализовать getReviewContextUseCase | TODO | Не начато | Реализация: IUseCase<{reviewId, repositoryId, changedFilePaths[]}, IReviewContext, DomainError>. CodeCityData → affectedDistricts → experts → hotspot check → drift check → ReviewRiskCalculator → warnings. Готово, если: для CORE-367 полностью выполнены пункты блока «Реализация», поведение подтверждено unit/integration тестами (happy-path, negative, edge-case), контракты DTO/ports совместимы, регрессии в смежных use case отсутствуют; DoD: `cd packages/core && bun run lint && bun run typecheck && bun test`. |
| CORE-368 | Реализовать suggestReviewersUseCase | TODO | Не начато | Реализация: IUseCase<{reviewId, repositoryId, changedFilePaths[], maxReviewers?}, ISuggestedReviewers, DomainError>. IKnowledgeMapProvider → ReviewerScorer(maxReviewers, default 3). Готово, если: для CORE-368 полностью выполнены пункты блока «Реализация», поведение подтверждено unit/integration тестами (happy-path, negative, edge-case), контракты DTO/ports совместимы, регрессии в смежных use case отсутствуют; DoD: `cd packages/core && bun run lint && bun run typecheck && bun test`. |
| CORE-369 | Реализовать tOKENS для Review Context | TODO | Не начато | Реализация: Добавить `TOKENS.Analysis.KnowledgeMapProvider`, `TOKENS.UseCases.GetReviewContext`, `TOKENS.UseCases.SuggestReviewers`. Готово, если: для CORE-369 полностью выполнены пункты блока «Реализация», поведение подтверждено unit/integration тестами (happy-path, negative, edge-case), контракты DTO/ports совместимы, регрессии в смежных use case отсутствуют; DoD: `cd packages/core && bun run lint && bun run typecheck && bun test`. |

---

## v0.63.1 — Comment Batching & Re-Review Tracking

> Batch-отправка комментариев через createReview() и tracking существующих комментариев при повторном review.

> **Результат версии:** Завершена версия «v0.63.1 — Comment Batching & Re-Review Tracking» в рамках M15; инкремент готовит продукт к стабильному end-to-end сценарию в своем слое.

| ID | Задача | Статус | Результат | Acceptance Criteria |
|----------|--------------------|--------|-----------|---------------------|
| CORE-374 | Реализовать iBatchReviewRequest | TODO | Не начато | Реализация: DTO: массив inline comments + CCR-level body для отправки одним API-вызовом через IGitProvider.createReview(). Max 30 comments per batch. Готово, если: для CORE-374 полностью выполнены пункты блока «Реализация», поведение подтверждено unit/integration тестами (happy-path, negative, edge-case), контракты DTO/ports совместимы, регрессии в смежных use case отсутствуют; DoD: `cd packages/core && bun run lint && bun run typecheck && bun test`. |
| CORE-375 | Реализовать iGitProvider.createReview() | TODO | Не начато | Реализация: Расширение порта: createReview(mrId, IBatchReviewRequest) → создаёт review с inline comments атомарно. GitHub: pulls/reviews API, GitLab: merge request notes. Готово, если: для CORE-375 полностью выполнены пункты блока «Реализация», поведение подтверждено unit/integration тестами (happy-path, negative, edge-case), контракты DTO/ports совместимы, регрессии в смежных use case отсутствуют; DoD: `cd packages/core && bun run lint && bun run typecheck && bun test`. |
| CORE-376 | Реализовать batch CreateFileCommentsStage | TODO | Не начато | Реализация: Stage 14 использует createReview() вместо postInlineComment() в цикле. Группирует suggestions в батчи по 30. Fallback на поштучную отправку при ошибке. Готово, если: для CORE-376 полностью выполнены пункты блока «Реализация», поведение подтверждено unit/integration тестами (happy-path, negative, edge-case), контракты DTO/ports совместимы, регрессии в смежных use case отсутствуют; DoD: `cd packages/core && bun run lint && bun run typecheck && bun test`. |
| CORE-377 | Реализовать iCommentTracker | TODO | Не начато | Реализация: Порт: findByReviewId(reviewId) → commentIds. save(reviewId, commentId, filePath, line). Для tracking созданных комментариев между review итерациями. Готово, если: для CORE-377 полностью выполнены пункты блока «Реализация», поведение подтверждено unit/integration тестами (happy-path, negative, edge-case), контракты DTO/ports совместимы, регрессии в смежных use case отсутствуют; DoD: `cd packages/core && bun run lint && bun run typecheck && bun test`. |
| CORE-378 | Реализовать re-review comment resolution | TODO | Не начато | Реализация: При повторном review: resolve/удалить outdated комментарии через IGitProvider. Обновить существующие если suggestion на ту же строку. Новые — создать. Готово, если: для CORE-378 полностью выполнены пункты блока «Реализация», поведение подтверждено unit/integration тестами (happy-path, negative, edge-case), контракты DTO/ports совместимы, регрессии в смежных use case отсутствуют; DoD: `cd packages/core && bun run lint && bun run typecheck && bun test`. |

---
